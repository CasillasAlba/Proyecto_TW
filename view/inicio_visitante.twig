{% extends 'index_view.twig' %}

{% block content %}
    <table class="table calendario">
            <thead>
                <tr>
                    <th>Vacuna</th>
                    <th>0 meses</th>
                    <th>2 meses</th>
                    <th>4 meses</th>
                    <th>11 meses</th>
                    <th>12 meses</th>
                    <th>15 meses</th>
                    <th>3 años</th>
                    <th>6 años</th>
                    <th>12 años</th>
                    <th>14 años</th>
                    <th>18 años</th>
                    <th>50 años</th>
                    <th>65 años</th>
                    <th>>65 años</th>
                </tr>
            </thead>
            <tbody>
                {% for entry in calendario %}
                    <tr>
                        <th>
                            {% if rol_user == "Admin" %}
                            <form action="index.php" method="POST" enctype="multipart/form-data">
                                <button type="submit" class="button-calendario-delete" name="eliminar-vacuna-recomendada">{{entry.Nombre}}</button>
                                <input hidden type="number" name="eliminar-vacuna-recomendada-id" value="{{ entry.ID }}">
                                <input hidden name="eliminar-vacuna-recomendada-acronimo" value="{{ entry.Acronimo }}">
                            </form>
                            {% else %}

                                {{entry.Nombre}}

                            {% endif %}
                        
                        </th>
                        {% if entry.Meses_ini <= 0 and entry.Meses_fin >= 0 or (entry.Meses_ini == 0 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 2 and entry.Meses_fin >= 2 or (entry.Meses_ini == 2 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 4 and entry.Meses_fin >= 4 or (entry.Meses_ini == 4 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 11 and entry.Meses_fin >= 11 or (entry.Meses_ini == 11 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 12 and entry.Meses_fin >= 12 or (entry.Meses_ini == 12 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 15 and entry.Meses_fin >= 15 or (entry.Meses_ini == 15 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 36 and entry.Meses_fin >= 36 or (entry.Meses_ini == 36 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 72 and entry.Meses_fin >= 72 or (entry.Meses_ini == 72 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 144 and entry.Meses_fin >= 144  or (entry.Meses_ini == 144 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 168 and entry.Meses_fin >= 168 or (entry.Meses_ini == 168 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 216 and entry.Meses_fin >= 216 or (entry.Meses_ini == 216 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 600 and entry.Meses_fin >= 600 or (entry.Meses_ini == 600 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_ini <= 780 and entry.Meses_fin >= 780  or (entry.Meses_ini == 780 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                        {% if entry.Meses_fin > 780 or (entry.Meses_ini == 780 and entry.Meses_fin == 0) %}
                            {% if entry.Tipo == "S" %}
                                <td class="fecha-vacuna">
                                    {% if entry.Sexo == "Masculino" %}
                                        {{entry.Acronimo}} (H)
                                    {% elseif entry.Sexo == "Femenino" %}
                                        {{entry.Acronimo}} (M)
                                    {% else %}
                                        {{entry.Acronimo}}
                                    {% endif %}
                                </td>
                            {% else %}
                                <td class="fecha-vacuna-sus">{{entry.Acronimo}}</td>
                            {% endif %}
                        {% else %}
                            <td></td>
                        {% endif %}
                    </tr>
                {% endfor %}
            </tbody>
        </table>
{% endblock %}

{% block login %}
    <section class="widget">
        <div class="bg-success">
            <p class="titulo-asides">Iniciar sesión</p>
        </div>
    
        <div>
            <form class="login-form" action="index.php" method="POST">
                <div class="mb-3">
                    <input type="text" name="usr" class="form-control" placeholder="DNI" id="loginUserInput"/>
                </div>
                <div class="mb-3"> 
                    <input type="password" name="psw" class="form-control" placeholder="Contraseña" id="loginPassInput"/>
                </div>
                    <input type="hidden" name="logueado" value="True"/>
                    <button class="btn btn-success" name="boton-login-visitante" type="submit">Iniciar Sesión</button>
            </form>
        </div>
    </section>
    <section class="widget">
        <div class="bg-success">
            <p class="titulo-asides">Registro</p>
        </div>
        <div>
            <form class="login-form" action='index.php' method="POST">
                <p>¿Aún no tienes cuenta?</p>
                <button class="btn btn-success" name="registrar-visitante" type="submit">Regístrate</button>
            </form>
        </div>
    </section>
{% endblock %}